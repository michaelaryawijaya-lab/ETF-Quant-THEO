# Strategy

The baseline strategy is implemented in `etf_futures_rv/strategy.py`. It has fixed, pre-declared parameters and uses a carry-normalized residual rather than parameter optimization or machine learning.
